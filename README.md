# PDF_combiner
self-study

This programme combines PDF files.
Call the function:
pdf_combiner("directory path(location of pdfs)")

Used libraries:
re (Regex)
os (handling files)
PyPDF2 (handling PDFs)
